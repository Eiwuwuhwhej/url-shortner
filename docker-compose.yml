services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: url_shortener_app
    restart: always
    ports:
      - "80:8000"
    volumes:
      - ./data:/app/data
